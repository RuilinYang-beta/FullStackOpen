{
  "name": "phonebook-backend-dummy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook-frontend && rm -rf dist && npm run build && cp -r dist ../../part3/phonebook-backend-dummy",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && fly deploy",
    "logs:prod": "fly logs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongodb": "^6.16.0",
    "mongoose": "^8.13.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
